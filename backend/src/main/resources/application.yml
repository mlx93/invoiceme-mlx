spring:
  application:
    name: invoiceme-backend
  
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/invoiceme}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        use_sql_comments: true
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true
  
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
  
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=5m
  
  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: America/Chicago
    default-property-inclusion: non_null

server:
  port: ${SERVER_PORT:8080}
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: on_param
    include-exception: false

logging:
  level:
    root: INFO
    com.invoiceme: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

jwt:
  secret: ${JWT_SECRET:lJ5Rz6L8EjrhNazBCeOcu+XCwQmgo+OvkDxafIwDCz8=}
  expiration: 86400000 # 24 hours in milliseconds

aws:
  region: ${AWS_REGION:us-east-1}
  access-key-id: ${AWS_ACCESS_KEY_ID:}
  secret-access-key: ${AWS_SECRET_ACCESS_KEY:}
  ses:
    from-email: ${AWS_SES_FROM_EMAIL:mylesethan93@gmail.com}
  s3:
    bucket-name: ${AWS_S3_BUCKET_NAME:invoiceme-pdfs-mlx}

app:
  timezone: America/Chicago
  late-fee:
    amount: 125.00
    max-months: 3

